# Web UI / dashboard
Flask==3.0.0
Werkzeug==3.0.1
Jinja2==3.1.4
itsdangerous==2.2.0
click==8.1.7
matplotlib
# Camera (Picamera2 stack)
picamera2==0.3.12
# libcamera installed via apt

# Audio logging
sounddevice==0.4.6
numpy==1.26.4

# System monitoring
psutil==5.9.8

# GPIO (using lgpio)
lgpio==0.2.2.0

# Serial (optional, if you add UART telemetry later)
pyserial==3.5

# JSON schema validation (optional)
jsonschema==4.23.0
